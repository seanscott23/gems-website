import React, { FC } from "react";
import { Button, Form, Modal } from "react-bootstrap";

const UpdateGemModal: FC = () => {
  return (
    <Modal>
      <Form></Form>
    </Modal>
  );
};

export default UpdateGemModal;
